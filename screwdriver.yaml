shared:
    image: node:6
cache:
    job:
        main: [/tmp/test]
jobs:
    main:
        steps:
            - cat: cat /tmp/test # should be empty first time
            - write: echo hello > /tmp/test
        requires: [~pr, ~commit]
